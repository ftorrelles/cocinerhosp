# SDD Configuration — CocinerHosp
# Auto-generated by sdd-init
version: 1.0.0

context: |
  CocinerHosp es una PWA mobile-first para gestión de producción en comedor hospitalario
  en Tenerife. Stack: React 18 + TypeScript strict + Vite + Tailwind CSS + Zustand +
  Supabase (auth + database + realtime) + React Router v6. Mobile-first, target ~390px.
  Usuarios: chefs en cocina industrial con guantes.

strict_tdd: false
strict_tdd_reason: "No package.json detected — project not yet scaffolded. Enable when vitest is installed."

testing:
  runner: null
  runner_reason: "Project scaffolding pending. No package.json found."
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: false
    type_checker: false
    formatter: false

persistence:
  mode: openspec
  directory: openspec/

phases:
  proposal:
    requires: []
  spec:
    requires: [proposal]
  design:
    requires: [proposal]
  tasks:
    requires: [spec, design]
  apply:
    requires: [tasks, spec, design]
  verify:
    requires: [spec, tasks, apply-progress]
  archive:
    requires: [all]
